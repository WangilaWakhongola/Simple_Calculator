version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    container_name: simple-calculator
    environment:
      - NODE_ENV=production
    volumes:
      - ./src:/app/src
      - /app/node_modules
